{
  "extends": "./tsconfig.json",
   "compilerOptions": {
    "noEmit": true, // Включаем emit для ESLint
    "composite": true // Включаем composite
  },
  "include": ["src/**/*", "vite.config.ts"],
}